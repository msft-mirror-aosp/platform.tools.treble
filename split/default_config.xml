<?xml version="1.0" encoding="utf-8"?>
<config>
    <!-- ============================================================= -->
    <!-- b/151670189: Makefiles referencing other projects from find-files-in-subdirs. -->
    <!-- test/vts/tools/build/tasks/vts_package.mk runs 'cd' on these dirs -->
    <add_project name="platform/test/vts-testcase/fuzz" />
    <add_project name="platform/test/vts-testcase/hal" />
    <add_project name="platform/test/vts-testcase/hal-trace" />
    <add_project name="platform/test/vts-testcase/kernel" />
    <add_project name="platform/test/vts-testcase/nbu" />
    <add_project name="platform/test/vts-testcase/performance" />
    <add_project name="platform/test/vts-testcase/vndk" />
    <!-- test/framework/build/Android.mk runs 'cd' on these dirs -->
    <add_project name="platform/test/vti/dashboard" />
    <add_project name="platform/test/vti/test_serving" />
    <add_project name="platform/tools/acloud" />
    <!-- ============================================================= -->

    <!-- ============================================================= -->
    <!-- b/151673809: Cross-project module type usage. -->
    <!-- external/crosvm/gpu_display uses module type wayland_protocol_codegen -->
    <add_project name="platform/external/wayland-protocols" />
    <!-- ============================================================= -->

    <!-- ============================================================= -->
    <!-- b/153651829: Symlinks that cross project boundaries -->
    <!-- frameworks/base has symlinks that point to packages/modules/ArtPrebuilt -->
    <add_project name="platform/packages/modules/ArtPrebuilt" />
    <!-- ============================================================= -->

    <!-- ============================================================= -->
    <!-- Needed for the repo tool. -->
    <add_project name="platform/tools/repohooks" />
    <!-- ============================================================= -->

    <!-- ============================================================= -->
    <!-- b/159710865: prebuilts/cmdline-tools dependency not visible to ninja. -->
    <add_project name="platform/prebuilts/cmdline-tools" />
    <!-- ============================================================= -->

    <!-- ============================================================= -->
    <!-- b/151879369: libc_jemalloc_wrapper's static_libs sources are not visible to ninja. -->
    <!-- "libc_jemalloc_wrapper" depends on undefined module "libjemalloc5" -->
    <add_project name="platform/external/jemalloc_new" />
    <!-- ============================================================= -->
</config>

